name: Repro C (schedule+dispatch with inputs)
on:
  schedule:
    - cron: "30 16 * * *"
  workflow_dispatch:
    inputs:
      dry:
        default: false
        type: boolean
jobs:
  job:
    runs-on: ubuntu-latest
    steps:
      - run: echo "repro-c ok"
