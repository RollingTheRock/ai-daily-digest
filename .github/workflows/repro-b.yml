name: Repro B (dispatch with inputs)
on:
  workflow_dispatch:
    inputs:
      dry:
        default: false
        type: boolean
      limit:
        default: "5"
        type: string
jobs:
  job:
    runs-on: ubuntu-latest
    steps:
      - run: echo "repro-b ok"
