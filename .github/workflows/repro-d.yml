name: Repro D (schedule+dispatch + environment)
on:
  schedule:
    - cron: "30 16 * * *"
  workflow_dispatch:
jobs:
  job:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - run: echo "repro-d ok"
